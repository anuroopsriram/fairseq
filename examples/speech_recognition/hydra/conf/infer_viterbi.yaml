# @package _group_

defaults:
    - task: null
    - model: null

hydra:
  run:
    dir: ${common_eval.results_path}/decode
  sweep:
    dir: ${common_eval.results_path}
    subdir: viterbi

decoding:
  exp_dir: ???
  decoder:
    name: viterbi

common_eval:
  results_path: ${decoding.exp_dir}/decode/${decoding.decoder.name}/${dataset.gen_subset}
  path: ${decoding.exp_dir}/checkpoint_best.pt
  post_process: letter
generation:
  nbest: 1
dataset:
  max_tokens: 1000000
  gen_subset: ???
